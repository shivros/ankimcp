# Acceptance Criteria

## COD-73: Missing mcp package

- [x] Packaged add-on vendors the `mcp` runtime dependency so imports succeed without external installs.
- [x] Automated suite passes via `rye run test`.
